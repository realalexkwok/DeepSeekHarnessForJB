# Item 15 (replanned) — Kilo-style context attachment chips: plan

1. Spec files (this directory).
2. DshChatPanel: chips bar + collapsible preview (scrollable, ~2 KB truncation);
   chip add/remove/toggle helpers; refresh hooks on document changes, context
   checkbox toggles, and send.
3. Full suite + buildPlugin + artifact.
