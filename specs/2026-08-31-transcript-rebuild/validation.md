# Item 5 follow-up / 17b — validation

## Automatic
- `./gradlew test` green (transcript unit tests + e2e unchanged and passing).
- `./gradlew buildPlugin` green; artifact path reported.
## Manual (host machine)
- Start a long streaming turn; scroll up mid-stream (wheel and thumb) — the
  view stays put with NO oscillation (the original item-5 limitation).
- Scrolling back to the bottom resumes following; text remains selectable.
- Tool cards, thinking disclosure, notices, and diff buttons still render.
