# Item 14 — Composer @-mentions: plan

1. Spec files (this directory).
2. PromptAssembly: `parseMentions`, `MentionedFile`, mention sections in
   `assemble`; unit tests.
3. DshChatPanel: document listener, bounded file walk, popup picker, token
   insertion, mention resolution in `gatherContext` (canonical confinement,
   caps).
4. Full suite + buildPlugin + artifact.
