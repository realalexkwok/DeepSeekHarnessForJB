# Item 16 — Settings simplification: plan

1. Spec files (this directory) + roadmap item 16.
2. DshSettingsConfigurable: drop the bundled-exe/base-url/model fields and
   rows; keep the embedded-runtime description; KEY_MASK handling in
   createComponent/isModified/apply/reset.
3. Build + full suite; artifact reported.
