# Item 18 — popup lists: plan

1. Spec files (this directory).
2. PopupListWindow: JWindow with search field + list, Enter/Escape/arrows,
   click-to-pick, focus-lost closes, positioned above the anchor.
3. Rewire the three composer tabs onto it (rows per requirements).
4. Full suite + buildPlugin + artifact.
