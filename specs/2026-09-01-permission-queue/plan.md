# Item 17c — permission FIFO queue + question-card audit: plan

1. Spec files (this directory).
2. DshRuntimeService: fair ReentrantLock serializes answerApproval (FIFO).
3. Full suite + buildPlugin + artifact.
